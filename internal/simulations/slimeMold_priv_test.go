package simulations

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestSlimeMoldPrivateMethods(t *testing.T) {
	t.Parallel()

	t.Run("SlimeMold.calculateNextFrame() sets appropriate next positions", func(t *testing.T) {
		assert.True(t, true)
	})

	t.Run("SlimeMold.applyNextFrame() applies appropriate next positions", func(t *testing.T) {
		assert.True(t, true)
	})
}
